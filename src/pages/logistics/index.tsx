import AgentScaffold from '../../components/AgentScaffold';

export default function LogisticsAgent() {
  return (
    <AgentScaffold
      category="Logistics"
      icon="truck"
      tint="amber"
      tagline="Delivery coordination, status updates, and route optimization calls — keep drivers and recipients informed."
    />
  );
}
