import AgentScaffold from '../../components/AgentScaffold';

export default function FinancialAgent() {
  return (
    <AgentScaffold
      category="Financial"
      icon="money"
      tint="green"
      tagline="Payment reminders, KYC verification, and loan screening workflows — compliant, traceable, and white-glove."
    />
  );
}
